import ComingNextShell from '../_components/ComingNextShell';

export default function DispatchInvoicesPage() {
  return (
    <ComingNextShell
      title="Invoices"
      description="Outbound billing for what you've dispatched. Distinct from the existing Invoices area, which covers inbound supplier invoices."
    />
  );
}
