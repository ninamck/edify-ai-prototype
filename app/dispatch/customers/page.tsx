import ComingNextShell from '../_components/ComingNextShell';

export default function DispatchCustomersPage() {
  return (
    <ComingNextShell
      title="Customers"
      description="Who this hub ships to — your spokes today, plus any external customers as the network grows. Contacts, delivery windows, and pricing tier per customer will live here."
    />
  );
}
