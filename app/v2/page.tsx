import HomeShell from '@/components/HomeShell';

/** Saved iteration — briefing + chat pills, recipe prefill, right panel (orders / sales / wastage). */
export default function V2Page() {
  return <HomeShell />;
}
