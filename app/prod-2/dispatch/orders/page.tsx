import ComingNextShell from '../_components/ComingNextShell';

export default function DispatchOrdersPage() {
  return (
    <ComingNextShell
      title="Customer orders"
      description="Inbound orders from spokes and any external customers, in one queue. Today these still flow through Production › Today; this tab will consolidate them once external customer intake lands."
    />
  );
}
