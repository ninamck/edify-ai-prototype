import HomeShell from '@/components/HomeShell';

/** Saved iteration v3 — frozen route; same shell as Latest at save time (shared Feed + sidebar). */
export default function V3Page() {
  return <HomeShell />;
}
