'use client';

// /production/prep — Burger King cold-prep sheet (sanitise / chop / prep of
// the veg that dresses every burger), tickable by station.

import PrepSheetScreen from '@/components/Production/PrepSheetScreen';

export default function ProductionPrepPage() {
  return <PrepSheetScreen />;
}
