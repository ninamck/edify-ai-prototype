// Production2 shim — the shortfall allocation maths are identical to the
// Original build, so we re-export rather than fork. Mirrors the same
// pattern as `components/Production2/fixtures.ts`.
export * from '../Production/dispatchShortfall';
